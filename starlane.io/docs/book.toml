[book]
title = "Documentation for starlane"
authors = ["Cevdet Arda Haran"]
language = "en"
multilingual = false
src = "src"

[build]
build-dir = "../target"                # the directory where the output is placed
create-missing = true             # whether or not to create missing pages
use-default-preprocessors = true  # use the default preprocessors
